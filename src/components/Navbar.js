import React from 'react';

const Navbar = () => {
  return (
    <div
      style={{
        height: '60px',
        backgroundColor: '#fff',
        boxShadow: '0px 2px 4px rgba(0,0,0,0.1)',
        display: 'flex',
        alignItems: 'center',
        padding: '0 16px',
        marginBottom: '16px',
      }}
    >
      <h2>Portal de Analise de Requisitos</h2>
    </div>
  );
};

export default Navbar;
